# 质检面板状态管理修复测试计划

## 修复内容概述
已修复`extractQualityCheckRecordsAutomatically`函数中的面板状态管理问题，确保在自动提取质检记录后能正确恢复面板的原始状态。

主要修改：
1. 在处理未找到质检记录弹窗的情况时，增加了完整的class状态恢复机制
2. 在处理找到质检记录弹窗的情况时，增加了调用popoverControl.restore()来恢复面板状态

## 测试步骤

### 1. 测试面板状态恢复功能
- 打开包含质检驳回信息的页面
- 确认质检详情面板初始状态为隐藏
- 触发自动提取功能（等待5秒或手动调用提取函数）
- 观察控制台输出，确认质检信息被正确提取
- 检查质检详情面板是否能正常关闭
- 验证用户手动点击面板显示/隐藏功能是否正常工作

### 2. 测试各种面板状态场景
- 测试页面加载时面板为隐藏状态的场景
- 测试面板已经显示时的场景
- 测试没有质检面板的场景

### 3. 验证提取内容的正确性
- 确认控制台输出的质检信息完整且正确
- 验证驳回理由提取是否准确（应为"融合度、歧字"）
- 检查提取的质检状态是否正确

## 预期结果
- 质检详情面板在自动提取后能正确恢复原始状态
- 用户可以正常手动操作面板显示/隐藏
- 质检信息能被正确提取并在控制台输出
- 不会出现面板无法关闭的问题

## 测试验证点
- [ ] 面板状态能正确恢复
- [ ] 用户操作不受影响
- [ ] 质检信息提取正确
- [ ] 无面板状态冲突问题