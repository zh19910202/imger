# 质检信息获取机制改进测试指南

## 改进内容概述
已改进`extractQualityCheckRecords`函数，使其能够在页面刚加载时正确提取隐藏的质检信息。

主要改进：
1. 使用更灵活的选择器查找质检弹窗，优先查找可见的，然后查找隐藏的
2. 增加了对特定class的popover的查找，特别是带有`custom-popover-with-lefter-arrow`和`w-full.md\\:w-96`类的面板
3. 添加了更完整的状态保存和恢复机制，确保在提取信息后能正确恢复面板的原始状态

## 测试步骤

### 1. 测试页面刚加载时的质检信息提取
- 打开包含质检驳回信息的页面
- 确保页面刚加载完成，不要手动点击任何质检详情按钮
- 按下'i'键显示数据模态框
- 检查模态框中是否显示了最新的质检驳回信息
- 验证驳回理由是否正确（应为"融合度，影子"或其他实际的驳回理由）

### 2. 测试手动交互后的质检信息提取
- 打开包含质检驳回信息的页面
- 手动点击显示质检详情面板
- 按下'i'键显示数据模态框
- 检查模态框中是否显示了最新的质检驳回信息
- 验证与步骤1中的信息是否一致

### 3. 测试面板状态恢复
- 打开包含质检驳回信息的页面
- 按下'i'键显示数据模态框
- 检查质检详情面板是否保持原始状态（隐藏状态应保持隐藏）
- 手动点击质检详情触发按钮，验证面板显示/隐藏功能是否正常工作

### 4. 测试各种页面状态
- 测试没有质检信息的页面
- 测试有质检信息但面板可见的页面
- 测试有质检信息但面板隐藏的页面

## 预期结果
- 页面刚加载时按下'i'键应能正确显示最新的质检驳回信息
- 驳回理由提取应准确无误
- 质检详情面板在自动提取后应能正确恢复原始状态
- 用户手动操作面板显示/隐藏功能应正常工作

## 测试验证点
- [ ] 页面刚加载时能正确提取质检信息
- [ ] 驳回理由提取准确
- [ ] 面板状态能正确恢复
- [ ] 用户操作不受影响