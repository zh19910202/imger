# 质检信息自动加载测试指南

## 测试目标
验证插件能够在不依赖用户手动交互的情况下，自动获取最新的质检驳回理由。

## 测试场景

### 场景1：页面加载后自动获取质检信息
1. 打开包含质检驳回信息的页面
2. 等待页面完全加载（约5-10秒）
3. 不进行任何手动操作
4. 按下'i'键显示数据模态框
5. 检查模态框中是否显示了最新的质检驳回理由

### 场景2：手动触发获取质检信息
1. 打开包含质检驳回信息的页面
2. 等待页面完全加载
3. 按下'i'键显示数据模态框
4. 检查模态框中是否显示了最新的质检驳回理由

### 场景3：用户手动交互后获取质检信息
1. 打开包含质检驳回信息的页面
2. 手动点击质检详情展开按钮
3. 按下'i'键显示数据模态框
4. 检查模态框中是否显示了最新的质检驳回理由
5. 验证与场景1和2的结果是否一致

## 预期结果
- 在所有场景中，模态框都应显示正确的质检驳回理由
- 驳回理由应为具体的描述，如"融合度，影子"等，而不是用户ID或状态信息
- 页面上的质检详情面板应在自动触发后恢复到原始状态

## 测试验证点
- [ ] 页面加载后能自动获取质检信息
- [ ] 按'i'键能获取质检信息
- [ ] 手动交互后能获取质检信息
- [ ] 驳回理由提取准确
- [ ] 页面元素状态正确恢复

## 调试信息
在测试过程中，请打开浏览器开发者工具查看控制台输出，应能看到以下日志：
- "[Appen Data Collector] 开始自动触发质检详情加载"
- "[Appen Data Collector] 模拟点击质检详情触发元素"
- "[Appen Data Collector] 模拟再次点击收起质检详情"
- 质检信息提取相关的日志信息